.DS_Store
__pycache__/
*.pyc
.venv/
.python-version
.uv-cache/
.mplconfig/

# Lean build artifacts (regenerated by `lake build`)
formal/.lake/

# LaTeX build intermediates (the compiled PDF is committed)
reports/*.aux
reports/*.log
reports/*.out
reports/*.fls
reports/*.fdb_latexmk
reports/*.blg
reports/*.synctex.gz

# Scratch figure runs (the committed outputs/figures and outputs/reports are kept)
outputs/dev/
outputs/run/
outputs/smoke/
